import Manifest from "sap/ui/core/Manifest";
/**
 * @namespace vss.com.dama.model
 */
export default class ManifestOverride extends Manifest {
    constructor(manifestObject: object, options?: object);
}
