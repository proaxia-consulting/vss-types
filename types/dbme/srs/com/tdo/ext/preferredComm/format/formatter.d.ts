import { ValueState } from "sap/ui/core/library";
declare const formatter: {
    matchingActionToValueState(action: string): ValueState;
};
/**
 * @global
 * @namespace dbme.srs.com.tdo.ext.preferredComm.format
 */
export default formatter;
